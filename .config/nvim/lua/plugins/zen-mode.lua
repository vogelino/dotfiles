---@type LazySpec
return {
  "folke/zen-mode.nvim",
  opts = {
    window = {
      width = 88, -- width will be 85% of the editor width
      options = {
        number = true,
        relativenumber = true,
      },
    },
  },
}
