# /Users/lucasvogel/.config/tmuxinator/wegweiser.yml

name: wegweiser
root: ~/repos/wegweiser-frontend

windows:
  - editor:
      layout: main-vertical
      panes:
        - tmux resize-pane -Z && e .
        - clear
  - scripts:
      panes:
        - npm run dev
