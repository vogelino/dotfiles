# /Users/lucasvogel/.config/tmuxinator/rewali-t3-stack.yml

name: Rewali (T3 Stack)
root: ~/repos/rewali-t3-stack

windows:
  - editor:
      panes:
        - tmux resize-pane -Z && e .
  - scripts:
      panes:
        - npm run dev
