name: ogestalten
root: ~/repos/oeffentliches-gestalten-strapi
windows:
  - CMS:
      layout: main-horizontal
      panes:
        - npm run develop
        - clear
  - Frontend:
      layout: main-horizontal
      panes:
        - cd ~/repos/oeffentliches-gestalten-astro && npm run dev -- --host 3001
        - cd ~/repos/oeffentliches-gestalten-astro && code -n .

