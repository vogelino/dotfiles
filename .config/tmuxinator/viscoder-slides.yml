# /Users/lucasvogel/.config/tmuxinator/viscoder-slides.yml

name: viscoder-slides
root: ~/repos/viscoders-slides

windows:
  - editor:
      layout: main-vertical
      panes:
        - tmux resize-pane -Z && e .
        - clear
  - scripts:
      panes:
        - npm run dev
